#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 *main - Entry point
 *
 *Return: 0
 *
 */
int main(void)
{
char lox, e, q;

e = 'e';
q = 'q';

for (low = 'a'; low <= 'z'; low++)
{
if (low != e && low != d)
putchar(low);
}
printf("\n");
return (0);
}
